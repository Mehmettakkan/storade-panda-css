import { semanticColors } from './colors'

export const semanticTokens = {
  colors: semanticColors,
  // Gelecekte ekleyebileceğiniz diğer semantik tokenlar
  // örneğin:
  // spacing: semanticSpacing,
  // sizes: semanticSizes,
}